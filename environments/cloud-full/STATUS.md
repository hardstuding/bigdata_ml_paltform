# cloud-full 当前部署状态

**这份文档记的是"现在实际到哪一步了",持续更新,不是一次性写完就不管。**
和 `README.md`(讲"接入 cloud-full 硬件通用该怎么做、给多少资源",不
随具体某一次部署变化)、[ADR-054](../../docs/decisions/054-cloud-full-bare-vm-bootstrap.md)
（讲"这次搭建过程中踩了哪些坑、为什么这么解决",记录过程和决策）是三份
不同性质的文档,不要混着改——具体哪条信息该记进哪份,看下面"三份文档
分别记什么"这一节。

如果你是接手这项工作的人(不管是人类还是别的 AI agent):先看这份文档
知道现在做到哪了,再去看 ADR-054 了解"为什么是这样",不用去翻对话记录
或者猜。

## 三份文档分别记什么

- **这份 `STATUS.md`**:当前这一次具体部署的实例信息、进度、下一步——
  会过时,下次换一台实例/重新部署时这份文档的内容大部分要重写。
- **`README.md`**:通用的"接入步骤"和"每个组件该给多少资源"参考——
  不随某一次具体部署变化,除非资源建议本身要调整。
- **ADR-054**:这次搭建过程中真实踩到的坑和解法——面向未来(尤其是
  以后迁移自建 IDC 时)复用,记录的是可复用的经验,不是这次的实例细节。

## 当前实例(2026-08-16 迁移到抢占式实例,原按量付费实例已释放)

- **云厂商/地域**:阿里云,华北6(乌兰察布),`cn-wulanchabu-a` 可用区
- **实例 ID**:`i-0jlbped4h1959tp591pe`(**不是**最初 2026-08-15 创建的
  `i-0jl7spqzz0rfnqv2abd2`——那台已经在 2026-08-16 释放,含它的两块盘,
  详细迁移过程见 `docs/CURRENT_WORK.md` 对应记录)
- **实例规格**:ecs.g9i.4xlarge(16 vCPU / 64GiB),x86_64——2026-08-15
  确认生产环境是 x86_64,cloud-full 验证环境按生产架构对齐,不是为了
  省钱用 ARM(那样会导致验证环境和生产架构不一致)
- **计费方式**:**抢占式实例**(`SpotStrategy=SpotAsPriceGo`,
  `SpotInterruptionBehavior=Stop`——被回收时只是停机不是销毁,数据安全,
  但停机后重开不保证能立刻抢到资源),约 ¥0.72/小时,比按量付费的
  ¥3.58/小时便宜约 80%。2026-08-15 最初创建时短暂用过抢占式(1小时保护期,
  当时判断风险太高改成按量付费),2026-08-16 重新评估后确认接受"可能
  临时开不了机"这个风险,正式换成抢占式,不是同一次决策的重复。
- **公网 IP**:`8.130.69.252`——和旧实例一样(旧实例释放后这个 IP 被
  新实例接手,纯巧合,不保证以后重启还是这个,不是固定 EIP)
- **磁盘**:系统盘 40GiB(ESSD PL0)+ 数据盘 200GiB(ESSD AutoPL),两块
  都设置了"不随实例释放"(`DeleteWithInstance=False`,迁移后手动确认过)
- **登录**:SSH 密钥对,私钥文件本地路径 `~/.ssh/cloud-full-key.pem`
  (只在操作者的 Mac 本地,不进 git;丢了 = 这个密钥对报废,阿里云不提供
  私钥找回,需要重新生成一对换上)——密钥对本身沿用的还是同一个
  `cloud-full-key`,迁移时新实例复用了这个密钥对,不用换。
- **带宽**:公网带宽按流量计费(不是固定带宽,入方向流量免费,出方向
  ¥0.8/GB)

## 进度清单

**2026-08-16 更新:下面这份清单是最初搭建阶段(2026-08-15)写的,当时
ArgoCD/PV/组件常驻这些都还没做——现在全部已经做完并且验证通过,清单
本身长期没跟着改,和实际状态不一致,已经具备误导接手人的风险,这次
一次性更正。往后这份文档只记"当前实例是什么/怎么连上",细粒度的
"做到哪一步了"交给 `docs/CURRENT_WORK.md`(会持续更新,不会像这里一样
过时),这里不重复维护第二份进度清单。**

- [x] 阿里云 ECS 实例创建、数据盘挂载、Docker、k3s——节点 Ready
- [x] 镜像缓存(68 个)导出+灌入云主机,增量续传验证过
- [x] SSH 隧道 + 独立 kubeconfig(`~/.kube/cloud-full-config`)
- [x] `scripts/00-generate-secrets.sh`
- [x] ArgoCD 安装 + root apps 接管(GitOps)
- [x] `enabled_components` 全部 43 个组件常驻(cloud-full 要求,不同于
      local-lite 的按需选择;2026-08-20 起改用 ADR-057 第三批的声明式
      机制,`pending-definitions/` 已退役)
- [x] 核心链路(Trino/Superset/Airflow)端到端验证通过——详见
      `docs/CURRENT_WORK.md` 任务 #12~#16 完成记录
- [x] 2026-08-16 抢占式实例迁移(省钱)+ 经济模式关机(RAM 角色 +
      看门狗自动调 `StopInstance --StoppedMode StopCharging`)
- [ ] 按 `README.md` 资源建议表精调各组件 resources(能跑,没有针对
      抢占式实例的规格做过专门优化)
- [ ] `docs/BACKLOG.md` P1 清单里的工程收口项(测试覆盖/CI 扩大/环境
      overlay 重构等,明确不是"从零拉起"这一步的范围)

## 过程中做过的关键决策(带原因,方便后来者理解"为什么"不只是"是什么")

- **抢占式实例 → 按量付费**:抢占式实例可能被随时收回,且当时默认设置
  只用 1 小时,跑不完这一整套部署和验证,改成按量付费(可以随时手动
  释放,不会被强制收走)。
- **固定带宽 → 按流量计费**:固定带宽 50Mbps 报价 ¥11.56/时,比实例
  本身(¥3.58/时)还贵,而且真正需要高速传输的是"上传镜像缓存"这个
  单次操作——查证过阿里云入方向流量完全免费,只有出方向按 ¥0.8/GB 收,
  改成按流量计费后合计从 ¥15.60/时 降到 ¥4.04/时,出方向流量按实际
  用量另计。
- **"安全加固"镜像 → 普通镜像**:担心额外的系统加固策略(防火墙/内核
  参数)和 K8s 的 NetworkPolicy/CNI 产生未知的相互影响,换成不带加固的
  普通 Ubuntu 26.04,减少一个不确定因素。
- **6443 端口不对公网开放**:管理这个集群走 SSH 隧道,不是把 K8s API
  server 直接暴露公网——见 ADR-054"安全考量"一节。
- **本机(colima)从 13G/6vCPU 缩到 6G/4vCPU,同时 park 掉本地的重量级
  组件**(Spark Operator/Airflow/Trino/OpenSearch/OpenMetadata):工作
  重心已经转到 cloud-full,本机不需要继续常驻这些,腾出来的内存给用户
  日常用电脑——这次操作过程中撞过 pod 卡在 Terminating(资源紧张导致
  优雅终止走不完,强制 `--grace-period=0` 清理容器)的问题,记进了
  [[feedback_destructive_ops]] 这条记忆,教训是"批量删除类操作要先明确
  目标清单再执行",不是这个项目自己的文档要记的内容,不在这里重复。
- **2026-08-15 收到 Codex 对这个项目的一轮 review**
  (`docs/claude-improvement-recommendations-2026-08-15.md`),指出的
  P0 级问题(按量资源开机门禁、破坏性操作缺防护、Trino 权限没有真正
  执行、cloud-full 基础设施脚本的具体 bug)已经采纳,处理方式和落地
  进度见 [ADR-055](../../docs/decisions/055-external-review-response-2026-08-15.md)
  ——其中数据盘选择那个具体 bug(注释和代码不一致,多块空盘可能格错盘)
  已经修复并在这台真实云主机上重新验证过,不影响已经做好的部分。

## 下一步(接手的人从这里继续)

**2026-08-16 更正**:这一节之前写的是"从零搭建阶段"的下一步(镜像缓存
灌入、装 ArgoCD),那一批早就做完了,不要照着执行一遍。真正的"现在该
干什么"以 `docs/CURRENT_WORK.md` 顶部为准——那份文档持续更新、不会
像这里一样过时。这里只留给"这台实例本身怎么连上"这类稳定信息:

1. 连接前先确认实例状态和当前公网 IP(不是固定 EIP,重启后可能变):
   `aliyun ecs DescribeInstances --profile cloud-full --InstanceIds
   '["i-0jlbped4h1959tp591pe"]'`。
2. SSH:`ssh -i ~/.ssh/cloud-full-key.pem root@<当前IP>`。
3. 操作集群:`KUBECONFIG=~/.kube/cloud-full-config kubectl ...`(SSH 隧道
   如果已断开,需要先重新建:`ssh -f -N -L
   16443:127.0.0.1:6443 -i ~/.ssh/cloud-full-key.pem root@<当前IP>`)。
4. 具体"接下来做什么"看 `docs/CURRENT_WORK.md` 的 CURRENT 和
   `docs/BACKLOG.md`,不在这份文档里重复维护第二套待办。
