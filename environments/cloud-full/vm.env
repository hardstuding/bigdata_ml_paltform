# cloud-full 那台云主机的身份。**开机/停机/迁移三个脚本共用这一份。**
#
# 为什么单独一个文件:2026-09-01 迁可用区时发现实例 ID 在
# scripts/32(开机)和 scripts/26(停机)里各写死一次。只改一个的后果是
# **停机脚本去停一台已经不存在的机器(静默成功),而新机器一直开着烧钱**
# —— 这类"看起来成功了"的失败,正是这个仓库被坑过最多次的形态。
#
# 不放进 config.yaml:那份是渲染 k8s 清单用的,和"哪台物理机"不是一回事。
CLOUD_VM_INSTANCE_ID=i-0jlbped4h1959tp591pe
CLOUD_VM_REGION=cn-wulanchabu
