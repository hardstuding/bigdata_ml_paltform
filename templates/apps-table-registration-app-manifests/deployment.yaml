# **这个文件是生成的**(2026-08-30 起)——源在
# templates/apps-table-registration-app-manifests/,改这里没用,
# 下一次 render-environment-config.py 会静默覆盖掉。
# 见 docs/decisions/043-table-registration-tool.md。2026-08-20 起(BACKLOG
# 2.1)用构建期固化依赖 + 源码的自建镜像(apps/table-registration-app/
# Dockerfile,GitHub Actions 推到 GHCR),不再是运行时 pip install 官方
# python:3.12-slim 镜像那套——见 apps/permission-request-app/deployment.yaml
# 顶部注释,同一个原因。
#
# OPENMETADATA_TOKEN:2026-08-22 起由 scripts/27-configure-openmetadata-bot.sh
# 自动建(部署阶段跑,不用人工去 OpenMetadata UI 建 bot)——复用
# OpenMetadata 安装时自带的 ingestion-bot 那个 unlimited JWT,从 Postgres
# 里读出来解密,建成 table-registration-app-openmetadata 这个 Secret(key
# 是 token)。想手动建/覆盖:
#     kubectl -n table-registration-app create secret generic table-registration-app-openmetadata \
#       --from-literal=token=<token>
#   没建这个 Secret 之前,表还是能正常建(Trino 建表这条路径不依赖它),
#   只是负责人/安全等级不会回写进 OpenMetadata,页面上会提示。
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: table-registration-app-data
  namespace: table-registration-app
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 256Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: table-registration-app
  namespace: table-registration-app
spec:
  replicas: 1
  strategy:
    type: Recreate  # ReadWriteOnce 的 PVC,同 permission-request-app 的理由
  selector:
    matchLabels:
      app: table-registration-app
  template:
    metadata:
      labels:
        app: table-registration-app
    spec:
      # 见 ADR-041(队列资源管理):人在等建表结果,归 interactive 档。
      priorityClassName: interactive
      containers:
        - name: app
          # 2026-08-20(BACKLOG 2.1):构建期固化依赖 + 源码,见
          # apps/table-registration-app/Dockerfile。这个 digest 是
          # linux/amd64+linux/arm64 多架构 manifest list 的 digest(见
          # apps/permission-request-app/deployment.yaml 顶部的详细说明,
          # 同一次重新构建),对应 commit
          # 8fbb640b396670cfdd08ae7beef4d322aae93421。
          image: crpi-t6h2mzjka4hzoldo.cn-hangzhou.personal.cr.aliyuncs.com/bigdata-platform/table-registration-app:fe73e92b7c46845bb569028426c2e00644b3ab4b
          env:
            - name: DB_PATH
              value: /data/registrations.db
            # 对账重试 CronJob 调 /internal/reconcile-openmetadata 用的共享
            # 密钥。optional: true —— 没建这个 Secret 时应用照常起,只是那两个
            # /internal 端点会拒绝所有请求(拒绝比误开安全)。
            # 表详情页上"申请访问"那个链接指向哪(ADR-086)。**指向平台的
            # 申请入口,不是直接指向 OA** —— 这个属性写进每一张表,OA 地址
            # 变一次要重写全部表实体;指向平台的话改的只有这一处。
            # 没配就不写那个链接(写一个指向 None 的链接比没有更糟)。
            # 2026-08-30 从"手工建的 secret key"改成按环境渲染。
            #
            # 原来是 `secretKeyRef ... optional: true`,而那个 key 从来
            # 没人建过 —— 于是 PERMISSION_APP_PUBLIC_URL 一直是空,写进
            # OpenMetadata 的 accessRequest 属性一直是空串,「从数据目录
            # 点一下去申请」这个功能**部署了但从来没工作过**,而且不报错。
            #
            # 这个地址完全能从环境配置推出来(和这个应用自己的 Ingress
            # host 是同一套拼法),没有任何理由让人手工去建一个 secret:
            # 少一件要记得做的事,就少一个"忘了做而且没人发现"的机会。
            - name: PERMISSION_APP_PUBLIC_URL
              value: "{{EXTERNAL_SCHEME}}://permission-request.{{DOMAIN_SUFFIX}}{{HTTP_PORT_SUFFIX}}"
            - name: INTERNAL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: table-registration-app-internal
                  key: token
                  optional: true
            - name: TRINO_HOST
              value: trino.trino.svc.cluster.local
            - name: TRINO_PORT
              value: "8443"
            - name: TRINO_USER
              value: table_registration_service
            - name: TRINO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trino-service-account
                  key: password-table_registration_service
            # 【可插拔基础设施,见 ADR-030】换外部 OpenMetadata 实例改这个 URL。
            - name: OPENMETADATA_URL
              value: http://openmetadata.openmetadata.svc.cluster.local:8585
            - name: OPENMETADATA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: table-registration-app-openmetadata
                  key: token
                  optional: true  # 没建这个 Secret 时,OPENMETADATA_TOKEN 就是空字符串,走降级路径
          ports:
            - containerPort: 8080
              name: http
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: table-registration-app-data
---
apiVersion: v1
kind: Service
metadata:
  name: table-registration-app
  namespace: table-registration-app
spec:
  selector:
    app: table-registration-app
  ports:
    - port: 8080
      targetPort: 8080
      name: http
