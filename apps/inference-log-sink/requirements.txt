flask==3.1.3
# **不能用 2.0.2**:它在 Python 3.12 上直接 import 失败
# (`No module named 'kafka.vendor.six.moves'` —— 那个 vendored six 在 3.12
# 下坏了)。2026-08-30 实机部署时才暴露:单元测试把 producer mock 掉了,
# 测不出 import 层面的问题。**这类"只有真部署才会暴露"的依赖问题,是
# 单测覆盖不到的那一类。**
kafka-python==3.0.11
