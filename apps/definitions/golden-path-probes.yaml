# 这个文件是自动生成的(python3 scripts/render-environment-config.py
# <env>),源头是 apps/components/golden-path-probes.yaml +
# environments/<env>/config.yaml 里的 enabled_components 列表——改动
# 请改 apps/components/golden-path-probes.yaml,不要直接改这份生成结果(下次渲染会被
# 覆盖)。这个组件要不要在这个环境启用,改 config.yaml 的
# enabled_components 列表,不要手动删/加这个文件。见
# docs/decisions/057-architecture-review-2026-08-19.md 第三批。
# 黄金链路探针(ADR-079)——按"一件真实的事能不能从头做到尾"探测,
# 不是按"某个进程活着"。
#
# 依赖 monitoring 命名空间里有 trino-service-account 这个 Secret
# (scripts/00-generate-secrets.sh 的 copy_secret 复制过去的)。
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: golden-path-probes
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/hardstuding/bigdata_ml_paltform.git
    targetRevision: main
    path: apps/golden-path-probes/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
